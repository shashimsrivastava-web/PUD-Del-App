rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // We allow read and write to all collections as a starting point.
    // In production, we'd add auth rules: if request.auth != null;
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
